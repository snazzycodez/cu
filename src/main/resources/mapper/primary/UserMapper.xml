<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.multidb.mapper.primary.UserMapper">

    <resultMap id="UserResultMap" type="com.example.multidb.entity.primary.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="full_name" property="fullName" jdbcType="VARCHAR"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, email, full_name, created_at, updated_at
    </sql>

    <select id="findAll" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        ORDER BY id
    </select>

    <select id="findById" parameterType="long" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.multidb.entity.primary.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, full_name, created_at, updated_at)
        VALUES (#{username}, #{email}, #{fullName}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.multidb.entity.primary.User">
        UPDATE users
        SET username = #{username},
            email = #{email},
            full_name = #{fullName},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>